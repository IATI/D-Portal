cd "$(dirname "$(readlink -f "$0")")"

if [ ! -d "/vagrant" ]; then

	echo $@
	vagrant up
	vagrant ssh -c " cd /host/vbox/ ; ./bin $* "

else

	export DSTORE_PG=postgresql:///dstore

	CMD=$1
	shift
	../bin/$CMD $@

fi
