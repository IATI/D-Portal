
# Add everything that has changed and commit it

cd `dirname $0`
cd ..

# these are generated so prevent conflicts by ignoreing them in this script
# add them manually after build if you want to update them

git checkout ctrack/js/ctrack.js
git checkout ctrack/js/ctrack.chunks.js
git checkout ctrack/js/ctrack.map

git checkout dstore/js/codes.js
git checkout dstore/json/iati_codes_to_name.json


git add . --all
git commit -a -m.
