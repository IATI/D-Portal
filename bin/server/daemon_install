cp daemon /etc/init.d/dportal
update-rc.d dportal defaults
update-rc.d dportal enable

echo "you should tell cron to run a check every few minutes"
echo "run crontab -e"
echo "and add the following line"
echo "*/2 * * * * /etc/init.d/dportal cron"
