if [ "$1" = "full" ] ; then
echo FULL > /var/tmp/dportal.update
fi

if [ "$1" = "mini" ] ; then
rm /var/tmp/dportal.update
fi


if [ -f /var/tmp/dportal.update ] ; then
	echo
	echo flag set to FULL
	echo
else
	echo
	echo flag set to MINI
	echo
fi

