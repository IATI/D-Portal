cd `dirname $0`

source settings

echo
echo You may now view the plated web site locally at:
echo
echo http://0.0.0.0:8000$PLATED_ROOT
echo

./node_modules/.bin/ecstatic --baseDir=$PLATED_ROOT --root=$PLATED_OUTPUT
